using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Level1Manager : MonoBehaviour {

	// Use this for initialization
	void Start () {
        BGMManager.instance.ToggleLoop(false);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
